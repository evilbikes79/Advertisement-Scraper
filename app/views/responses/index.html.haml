:javascript
%h1 Listing responses

%table
  %tr
    %th Search
    %th Response html
    %th
    %th
    %th

  - @responses.each do |response|
    %tr
      %td= response.search_id
      %td= response.response_html
      %td= link_to 'Show', response
      %td= link_to 'Edit', edit_response_path(response)
      %td= link_to 'Destroy', response, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Response', new_response_path
